{
  "id": "coding_steward/cross_workspace_routing",
  "category": "coding_steward",
  "description": "Validate that a realistic steward turn targeting another local workspace surfaces delegation guidance instead of inline cross-workspace shell execution.",
  "blocking": true,
  "command": "cargo test -p alan --test smoke_test smoke_cross_workspace_reading_surfaces_workspace_delegation_path -- --exact",
  "assertions": [
    "a cross-workspace local bash read returns requires_workspace_delegation",
    "the runtime does not escalate through a confirmation yield for routing failures",
    "the steward prompt exposes workspace-inspect guidance with explicit workspace_root and optional cwd launch inputs"
  ],
  "kpi_tags": [
    "workspace_routing",
    "delegation",
    "user_realism"
  ]
}
