{
  "id": "autonomy/scheduler_wake",
  "category": "autonomy",
  "description": "Validate sleep/wake run transitions and scheduler wake checkpoint persistence.",
  "blocking": true,
  "command": "cargo test -p alan daemon::state::tests::sleep_until_transitions_run_to_sleeping_and_sets_wake -- --exact",
  "assertions": [
    "sleep_until persists waiting schedule",
    "run transitions to sleeping with next_wake_at",
    "sleep_until checkpoint is recorded"
  ],
  "kpi_tags": [
    "scheduler",
    "wake",
    "state_transition"
  ]
}
