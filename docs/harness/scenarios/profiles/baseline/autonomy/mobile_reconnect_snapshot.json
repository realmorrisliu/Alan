{
  "id": "autonomy/mobile_reconnect_snapshot",
  "category": "autonomy",
  "description": "Validate reconnect snapshot returns dedupe hints and actionable pending-yield recovery state.",
  "blocking": false,
  "command": "cargo test -p alan daemon::routes::tests::reconnect_snapshot_returns_replay_and_pending_yield_signal -- --exact",
  "assertions": [
    "reconnect snapshot exposes latest_submission_id for dedupe hints",
    "execution snapshot preserves yielded + await_user_resume state",
    "pending-yield signal is emitted as informational notification"
  ],
  "kpi_tags": [
    "mobile",
    "reconnect",
    "snapshot"
  ]
}
