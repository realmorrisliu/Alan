{
  "id": "autonomy/mobile_notification_signal",
  "category": "autonomy",
  "description": "Validate reconnect snapshot maps structured-input yields to informational notification signal types.",
  "blocking": false,
  "command": "cargo test -p alan daemon::routes::tests::reconnect_snapshot_maps_structured_input_signal_type -- --exact",
  "assertions": [
    "structured_input yield maps to pending_structured_input signal type",
    "signal carries request_id needed for UX resume affordance",
    "notification signal remains informational-only"
  ],
  "kpi_tags": [
    "mobile",
    "notification",
    "yield_signal"
  ]
}
