{
  "id": "autonomy/dedup_side_effect",
  "category": "autonomy",
  "description": "Validate side-effect idempotency dedupe survives rollout recovery and skips duplicate execution.",
  "blocking": true,
  "command": "cargo test -p alan-runtime runtime::tool_orchestrator::tests::test_side_effect_dedupe_survives_session_rollout_recovery_for_file_effects -- --exact",
  "assertions": [
    "same logical effect does not execute twice after recovery",
    "dedupe_hit path emits auditable tool completion payload",
    "effect record continuity survives session reload"
  ],
  "kpi_tags": [
    "dedupe",
    "idempotency",
    "side_effect_safety"
  ]
}
