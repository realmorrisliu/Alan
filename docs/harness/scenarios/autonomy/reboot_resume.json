{
  "id": "autonomy/reboot_resume",
  "category": "autonomy",
  "description": "Validate run restore snapshot continuity used by reboot/resume recovery flow.",
  "blocking": true,
  "command": "cargo test -p alan daemon::state::tests::restore_run_returns_latest_checkpoint_snapshot -- --exact",
  "assertions": [
    "restore_run returns latest checkpoint snapshot",
    "yielded run state maps to await_user_resume action",
    "run snapshot continuity is preserved across restore path"
  ],
  "kpi_tags": [
    "recovery",
    "checkpoint",
    "continuity"
  ]
}
