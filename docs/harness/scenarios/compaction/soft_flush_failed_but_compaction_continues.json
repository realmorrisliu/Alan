{
  "id": "compaction/soft_flush_failed_but_compaction_continues",
  "category": "compaction",
  "description": "Validate a soft-threshold memory flush failure emits warning telemetry without blocking the following compaction attempt or turn completion.",
  "blocking": true,
  "command": "cargo test -p alan --test compaction_integration_test compaction_auto_pre_turn_soft_flush_failure_surfaces_match -- --exact",
  "assertions": [
    "memory flush failure emits warning telemetry but remains durable and externally visible",
    "compaction still completes successfully after the flush failure",
    "failed flush attempt remains linked from the compaction attempt"
  ],
  "kpi_tags": [
    "memory_flush_failure",
    "soft_threshold",
    "warning_continuation"
  ]
}
