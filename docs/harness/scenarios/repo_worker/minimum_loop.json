{
  "id": "repo_worker/minimum_loop",
  "category": "repo_worker",
  "description": "Validate the first-party repo-worker package runs task->plan->edit->verify->deliver without forking the runtime contract.",
  "blocking": true,
  "command": "bash scripts/repo-worker/run_smoke.sh --mode ci",
  "assertions": [
    "repo-worker package layout is complete",
    "minimum repo-worker loop artifacts are emitted",
    "verification command exits zero",
    "delivery contract and evaluator boundary validations pass"
  ],
  "kpi_tags": [
    "repo_worker_loop",
    "package_shape",
    "verification",
    "delivery_contract"
  ]
}
