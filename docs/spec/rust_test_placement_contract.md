# Legacy Bridge: Rust Test Placement Contract

This path is not the authoritative contract source.

Authoritative OpenSpec owner:

- `openspec/specs/rust-test-placement-contract/spec.md`

Keep new Rust test placement requirements in OpenSpec. Remove this bridge once
active references stop using the legacy `docs/spec/rust_test_placement_contract.md`
path.
