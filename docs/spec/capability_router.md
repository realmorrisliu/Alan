# Legacy Bridge: Capability Router

This path is not the authoritative contract source.

Authoritative OpenSpec owners:

- `openspec/changes/harden-agent-operating-system-contracts/specs/agent-capability-routing/spec.md`
- `openspec/specs/governance-tooling-contract/spec.md`
- `openspec/specs/skill-system-contract/spec.md`

Keep new capability-routing requirements in OpenSpec. Remove this bridge once
active references stop using the legacy `docs/spec/capability_router.md` path.
