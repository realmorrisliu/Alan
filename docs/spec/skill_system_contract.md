# Legacy Bridge: Skill System Contract

This path is not the authoritative contract source.

Authoritative OpenSpec owner:

- `openspec/specs/skill-system-contract/spec.md`

Current skill implementation and operator guidance remain in
`docs/skills_and_tools.md` and `docs/skill_authoring.md`.

Keep new skill-system requirements in OpenSpec. Remove this bridge once active
references stop using the legacy `docs/spec/skill_system_contract.md` path.
